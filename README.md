# Deep Rubiks

```bash
# Construction des images Docker
docker-compose build

# Lancement des conteneurs
docker-compose up

# Ou en une seule commande (build + run)
docker-compose up --build

# Pour lancer en arrière-plan
docker-compose up -d
```